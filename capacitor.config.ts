import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.proyectoultimounidad2',
  appName: 'proyectoultimounidad2',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
